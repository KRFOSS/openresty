--- a/src/ngx_http_lua_cache.c
+++ b/src/ngx_http_lua_cache.c
@@ -347,6 +347,88 @@ error:
 }
 
 
+ngx_int_t
+ngx_http_lua_rokfoss_test_inline(ngx_conf_t *cf, const char *tag,
+    const u_char *src, size_t src_len)
+{
+    int          rc;
+    lua_State   *L;
+    const char  *err;
+
+    if (!ngx_test_config) {
+        return NGX_OK;
+    }
+
+    L = luaL_newstate();
+    if (L == NULL) {
+        ngx_conf_log_error(NGX_LOG_EMERG, cf, 0,
+                           "Lua 문법 검사용 VM을 초기화하지 못했습니다");
+        return NGX_ERROR;
+    }
+
+    luaL_openlibs(L);
+
+    rc = ngx_http_lua_clfactory_loadbuffer(L, (char *) src, src_len, tag);
+    if (rc != 0) {
+        err = lua_isstring(L, -1) ? lua_tostring(L, -1) : "unknown error";
+
+        ngx_conf_log_error(NGX_LOG_EMERG, cf, 0,
+                           "Lua 문법 검사에 실패했습니다 (%s): %s",
+                           tag, err);
+        lua_close(L);
+        return NGX_ERROR;
+    }
+
+    lua_close(L);
+    return NGX_OK;
+}
+
+
+ngx_int_t
+ngx_http_lua_rokfoss_test_file(ngx_conf_t *cf, const u_char *src,
+    size_t src_len)
+{
+    int          rc;
+    u_char      *script;
+    lua_State   *L;
+    const char  *err;
+
+    if (!ngx_test_config) {
+        return NGX_OK;
+    }
+
+    script = ngx_http_lua_rebase_path(cf->pool, (u_char *) src, src_len);
+    if (script == NULL) {
+        ngx_conf_log_error(NGX_LOG_EMERG, cf, 0,
+                           "Lua 파일 경로를 확인하지 못했습니다");
+        return NGX_ERROR;
+    }
+
+    L = luaL_newstate();
+    if (L == NULL) {
+        ngx_conf_log_error(NGX_LOG_EMERG, cf, 0,
+                           "Lua 파일 검사용 VM을 초기화하지 못했습니다");
+        return NGX_ERROR;
+    }
+
+    luaL_openlibs(L);
+
+    rc = ngx_http_lua_clfactory_loadfile(L, (char *) script);
+    if (rc != 0) {
+        err = lua_isstring(L, -1) ? lua_tostring(L, -1) : "unknown error";
+
+        ngx_conf_log_error(NGX_LOG_EMERG, cf, 0,
+                           "Lua 파일 검사에 실패했습니다 (\"%s\"): %s",
+                           script, err);
+        lua_close(L);
+        return NGX_ERROR;
+    }
+
+    lua_close(L);
+    return NGX_OK;
+}
+
+
 u_char *
 ngx_http_lua_gen_chunk_cache_key(ngx_conf_t *cf, const char *tag,
     const u_char *src, size_t src_len)
@@ -355,6 +431,10 @@ ngx_http_lua_gen_chunk_cache_key(ngx_conf_t *cf, const char *tag,
     u_char      *p, *out;
     size_t       tag_len;
 
+    if (ngx_http_lua_rokfoss_test_inline(cf, tag, src, src_len) != NGX_OK) {
+        return NULL;
+    }
+
     tag_len = ngx_strlen(tag);
 
     out = ngx_palloc(cf->pool, tag_len + NGX_HTTP_LUA_INLINE_KEY_LEN + 2);
@@ -398,6 +478,10 @@ ngx_http_lua_gen_file_cache_key(ngx_conf_t *cf, const u_char *src,
 {
     u_char      *out;
 
+    if (ngx_http_lua_rokfoss_test_file(cf, src, src_len) != NGX_OK) {
+        return NULL;
+    }
+
     out = ngx_palloc(cf->pool, NGX_HTTP_LUA_FILE_KEY_LEN + 1);
     if (out == NULL) {
         return NULL;
--- a/src/ngx_http_lua_cache.h
+++ b/src/ngx_http_lua_cache.h
@@ -16,5 +16,9 @@ ngx_int_t ngx_http_lua_cache_loadfile(ngx_log_t *log, lua_State *L,
     const u_char *script, int *cache_ref, const u_char *cache_key);
+ngx_int_t ngx_http_lua_rokfoss_test_inline(ngx_conf_t *cf, const char *tag,
+    const u_char *src, size_t src_len);
+ngx_int_t ngx_http_lua_rokfoss_test_file(ngx_conf_t *cf, const u_char *src,
+    size_t src_len);
 u_char *ngx_http_lua_gen_chunk_cache_key(ngx_conf_t *cf, const char *tag,
     const u_char *src, size_t src_len);
 u_char *ngx_http_lua_gen_file_cache_key(ngx_conf_t *cf, const u_char *src,
     size_t src_len);
--- a/src/ngx_http_lua_directive.c
+++ b/src/ngx_http_lua_directive.c
@@ -1373,6 +1373,11 @@ ngx_http_lua_init_by_lua(ngx_conf_t *cf, ngx_command_t *cmd,
     lmcf->init_handler = (ngx_http_lua_main_conf_handler_pt) cmd->post;
 
     if (cmd->post == ngx_http_lua_init_by_file) {
+        if (ngx_http_lua_rokfoss_test_file(cf, value[1].data,
+                                           value[1].len) != NGX_OK)
+        {
+            return NGX_CONF_ERROR;
+        }
         name = ngx_http_lua_rebase_path(cf->pool, value[1].data,
                                         value[1].len);
         if (name == NULL) {
@@ -1384,6 +1389,12 @@ ngx_http_lua_init_by_lua(ngx_conf_t *cf, ngx_command_t *cmd,
         lmcf->init_src.len = ngx_strlen(name);
 
     } else {
+        if (ngx_http_lua_rokfoss_test_inline(cf, "init_by_lua",
+                                             value[1].data, value[1].len)
+            != NGX_OK)
+        {
+            return NGX_CONF_ERROR;
+        }
         lmcf->init_src = value[1];
 
         chunkname = ngx_http_lua_gen_chunk_name(cf, "init_by_lua",
@@ -1433,6 +1444,11 @@ ngx_http_lua_init_worker_by_lua(ngx_conf_t *cf, ngx_command_t *cmd,
     lmcf->init_worker_handler = (ngx_http_lua_main_conf_handler_pt) cmd->post;
 
     if (cmd->post == ngx_http_lua_init_worker_by_file) {
+        if (ngx_http_lua_rokfoss_test_file(cf, value[1].data,
+                                           value[1].len) != NGX_OK)
+        {
+            return NGX_CONF_ERROR;
+        }
         name = ngx_http_lua_rebase_path(cf->pool, value[1].data,
                                         value[1].len);
         if (name == NULL) {
@@ -1444,6 +1460,12 @@ ngx_http_lua_init_worker_by_lua(ngx_conf_t *cf, ngx_command_t *cmd,
         lmcf->init_worker_src.len = ngx_strlen(name);
 
     } else {
+        if (ngx_http_lua_rokfoss_test_inline(cf, "init_worker_by_lua",
+                                             value[1].data, value[1].len)
+            != NGX_OK)
+        {
+            return NGX_CONF_ERROR;
+        }
         lmcf->init_worker_src = value[1];
 
         chunkname = ngx_http_lua_gen_chunk_name(cf, "init_worker_by_lua",
@@ -1494,6 +1516,11 @@ ngx_http_lua_exit_worker_by_lua(ngx_conf_t *cf, ngx_command_t *cmd,
     lmcf->exit_worker_handler = (ngx_http_lua_main_conf_handler_pt) cmd->post;
 
     if (cmd->post == ngx_http_lua_exit_worker_by_file) {
+        if (ngx_http_lua_rokfoss_test_file(cf, value[1].data,
+                                           value[1].len) != NGX_OK)
+        {
+            return NGX_CONF_ERROR;
+        }
         name = ngx_http_lua_rebase_path(cf->pool, value[1].data,
                                         value[1].len);
         if (name == NULL) {
@@ -1505,6 +1532,12 @@ ngx_http_lua_exit_worker_by_lua(ngx_conf_t *cf, ngx_command_t *cmd,
         lmcf->exit_worker_src.len = ngx_strlen(name);
 
     } else {
+        if (ngx_http_lua_rokfoss_test_inline(cf, "exit_worker_by_lua",
+                                             value[1].data, value[1].len)
+            != NGX_OK)
+        {
+            return NGX_CONF_ERROR;
+        }
         lmcf->exit_worker_src = value[1];
 
         chunkname = ngx_http_lua_gen_chunk_name(cf, "exit_worker_by_lua",
