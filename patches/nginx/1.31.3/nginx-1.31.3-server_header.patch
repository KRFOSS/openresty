--- a/src/core/nginx.h
+++ b/src/core/nginx.h
@@ -11,10 +11,10 @@
 
 #define nginx_version      1031003
 #define NGINX_VERSION      "1.31.3"
-#define NGINX_VER          "nginx/" NGINX_VERSION
+#define NGINX_VER          "ROKFOSS-Shield/" NGINX_VERSION
 
 #ifdef NGX_BUILD
-#define NGINX_VER_BUILD    NGINX_VER " (" NGX_BUILD ")"
+#define NGINX_VER_BUILD    NGINX_VER
 #else
 #define NGINX_VER_BUILD    NGINX_VER
 #endif
--- a/src/http/ngx_http_header_filter_module.c
+++ b/src/http/ngx_http_header_filter_module.c
@@ -47,7 +47,7 @@
 };
 
 
-static u_char ngx_http_server_string[] = "Server: nginx" CRLF;
+static u_char ngx_http_server_string[] = "Server: ROKFOSS-Shield" CRLF;
 static u_char ngx_http_server_full_string[] = "Server: " NGINX_VER CRLF;
 static u_char ngx_http_server_build_string[] = "Server: " NGINX_VER_BUILD CRLF;
 
--- a/src/http/v2/ngx_http_v2_filter_module.c
+++ b/src/http/v2/ngx_http_v2_filter_module.c
@@ -120,7 +120,10 @@
     ngx_http_core_srv_conf_t  *cscf;
     u_char                     addr[NGX_SOCKADDR_STRLEN];
 
-    static const u_char nginx[5] = { 0x84, 0xaa, 0x63, 0x55, 0xe7 };
+    static const u_char rokfoss_shield[15] = {
+        0x0e, 'R', 'O', 'K', 'F', 'O', 'S', 'S',
+        '-', 'S', 'h', 'i', 'e', 'l', 'd'
+    };
 #if (NGX_HTTP_GZIP)
     static const u_char accept_encoding[12] = {
         0x8b, 0x84, 0x84, 0x2d, 0x69, 0x5b, 0x05, 0x44, 0x3c, 0x86, 0xaa, 0x6f
@@ -232,7 +235,7 @@
             len += 1 + nginx_ver_build_len;
 
         } else {
-            len += 1 + sizeof(nginx);
+            len += 1 + sizeof(rokfoss_shield);
         }
     }
 
@@ -456,7 +459,8 @@
 
         } else {
             ngx_log_debug0(NGX_LOG_DEBUG_HTTP, fc->log, 0,
-                           "http2 output header: \"server: nginx\"");
+                           "http2 output header: \"server: "
+                           "ROKFOSS-Shield\"");
         }
 
         *pos++ = ngx_http_v2_inc_indexed(NGX_HTTP_V2_SERVER_INDEX);
@@ -481,7 +485,7 @@
             pos = ngx_cpymem(pos, nginx_ver_build, nginx_ver_build_len);
 
         } else {
-            pos = ngx_cpymem(pos, nginx, sizeof(nginx));
+            pos = ngx_cpymem(pos, rokfoss_shield, sizeof(rokfoss_shield));
         }
     }
 
--- a/src/http/v3/ngx_http_v3_filter_module.c
+++ b/src/http/v3/ngx_http_v3_filter_module.c
@@ -169,7 +169,7 @@
             n = sizeof(NGINX_VER_BUILD) - 1;
 
         } else {
-            n = sizeof("nginx") - 1;
+            n = sizeof("ROKFOSS-Shield") - 1;
         }
 
         len += ngx_http_v3_encode_field_lri(NULL, 0,
@@ -352,8 +352,8 @@
             n = sizeof(NGINX_VER_BUILD) - 1;
 
         } else {
-            p = (u_char *) "nginx";
-            n = sizeof("nginx") - 1;
+            p = (u_char *) "ROKFOSS-Shield";
+            n = sizeof("ROKFOSS-Shield") - 1;
         }
 
         ngx_log_debug2(NGX_LOG_DEBUG_HTTP, c->log, 0,
